<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="${appLabel}"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="${appLabel}"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permisos -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camara: el boton "Tomar foto" de los comprobantes lanza
         ACTION_IMAGE_CAPTURE. Al declarar el permiso, Capacitor lo pide en
         tiempo de ejecucion antes de abrir la camara; ver
         isMediaCaptureSupported en BridgeWebChromeClient. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Guardar los PDF y Excel en la carpeta Descargas. Desde Android 10 se
         escribe por MediaStore y no hace falta permiso; en Android 9 y
         anteriores si. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- La camara no es obligatoria para instalar: sin esto Play Store excluye
         a los equipos que no la tienen. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Desde Android 11 una app solo "ve" los paquetes que declara. Sin este
         bloque, resolveActivity() devuelve null, Capacitor concluye que no hay
         camara disponible y cae al selector de archivos sin ningun aviso
         (showImageCapturePicker -> "Falling back to default file picker"). -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>
</manifest>
